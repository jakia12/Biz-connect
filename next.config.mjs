/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'via.placeholder.com', 'i.ibb.co', 'i.ibb.co.com', 'placehold.co'],
  },
};

export default nextConfig;
